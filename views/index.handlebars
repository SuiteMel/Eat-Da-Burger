<div>

<h1>Eat-Da-Burger!</h1>

<ul class="devourIt">
  {{#each burgers}}
    {{#unless devoured}}
      <li class="devourable">{{id}}. {{burger_name}} <button class="devour" data-id="{{id}}" data-newdevour="{{devoured}}">Devour It!</button></li>
    {{/unless}}
  {{/each}}
</ul>

<ul class="itDevoured">
  {{#each burgers}}
    {{#if devoured}}
      <li class="devoured">{{id}}. {{burger_name}}</li>
    {{/if}}
  {{/each}}
</ul>

<form class="create-form">
  <div class="form-group">
    <input type="text" name="burger_name" id="burg"></input>
  </div>
  <button type="submit">Submit</button>
  <br />
  <label for="burger_name">Enter a burger you want to eat Click the "Submit" button.</label>
</form>

</div>